import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderBottomColor: '#e2b497',
    borderBottomWidth: 2,
    padding: 6,
    marginHorizontal: 12,
    marginVertical: 4,
  },

  title: {
    fontWeight: 'bold',
    fontSize: 18,
    color: '#e2b497',
    textAlign: 'center',
  },
});
